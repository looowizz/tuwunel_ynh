This upgrade introduces the ability for this package to automatically set-up and configure audio/video calls. This is disabled by default.

If this is something you'd be interested in, please run `sudo yunohost app setting tuwunel calls_enabled -v 1` via ssh before upgrading
